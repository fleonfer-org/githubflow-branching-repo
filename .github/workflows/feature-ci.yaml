name: 'CI workflow for feature branches'

on:
  push:
    branches: ['feature/**']
    paths-ignore:
      - '.github/**'
  pull_request:

jobs:
  CI:
    runs-on: ubuntu-latest
    steps:
      - run: echo "CI workflow for feature branches..."