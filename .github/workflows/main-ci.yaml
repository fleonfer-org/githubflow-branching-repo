name: 'CI workflow for main branch'

on:
  push:
    branches: 
      - 'main'
    paths-ignore:
      - '.github/**'

jobs:
  CI:
    runs-on: ubuntu-latest
    steps:
      - run: echo "CI workflow for main branch..."